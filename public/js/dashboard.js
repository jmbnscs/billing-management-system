$(document).ready( function () {
    //to hash condition
    console.log(hashed);
    if (hashed == 0) { 
        window.location.replace('../views/profile.php');
    }
});

// $(window).on('load', function () {

// });

